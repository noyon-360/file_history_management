name: file_history_management
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  get: ^4.7.3
  flutx_core: ^1.1.1+1
  hive_flutter: ^1.1.0
  flutter_secure_storage: ^10.0.0
  simplest_document_scanner: ^1.1.4
  path_provider: ^2.1.1
  open_filex: ^4.3.4
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true